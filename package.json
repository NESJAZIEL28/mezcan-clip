{
  "name": "mezcan-clip-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Capacitor live URL mode - no build needed'"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
